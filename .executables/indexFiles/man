index-files
———————————

Usage:
	$ index-files slides. --start-from 2 --filter \.png$ --dry-run

Argumets:
	Arguments will be concatinates into file name suffix

For full options, see `index-files --help`
