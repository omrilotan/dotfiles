# To install or remove components at your current SDK version [367.0.0], run:
#   $ gcloud components install COMPONENT_ID
#   $ gcloud components remove COMPONENT_ID
#
# To update your SDK installation to the latest version [367.0.0], run:
#   $ gcloud components update
#
#
#
# To take a quick anonymous survey, run:
#   $ gcloud survey
export PATH="$PATH:$HOME/bin/google-cloud-sdk/bin"

# The next line updates PATH for the Google Cloud SDK.
if [ -f '/Users/omri.lotan/bin/google-cloud-sdk/path.bash.inc' ]; then . '/Users/omri.lotan/bin/google-cloud-sdk/path.bash.inc'; fi

# The next line enables shell command completion for gcloud.
if [ -f '/Users/omri.lotan/bin/google-cloud-sdk/completion.bash.inc' ]; then . '/Users/omri.lotan/bin/google-cloud-sdk/completion.bash.inc'; fi
